# ESP32-WiFi-Network-Monitoring-System
ESP32 project developed with ESP-IDF for Wi-Fi network monitoring. The system automatically connects to a wireless network, monitors connection status, displays network information (IP address, subnet mask, gateway), and uses LED indicators to signal connection and disconnection events in real time.
